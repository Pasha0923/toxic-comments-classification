version: "3.9"

services:

  toxic-app:

    build: .

    container_name: toxic-comments-classifier

    ports:
      - "8501:8501"

    volumes:
      - ./models:/app/models

    restart: unless-stopped